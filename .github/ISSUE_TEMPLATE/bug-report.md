---
name: Bug report
about: Anything else — a crash, a picture defect, a control that does not work
title: ""
labels: ""
---

**Release version and OS:**

**What happened, and what you expected:**

**Attach `cw_runtime.log`** (written on every run beside the game's `assets/` folder,
or beside the `.AppImage`; the run before is `cw_runtime.log.1`). For a crash it ends
with the crash report. For "which GPU/driver/display path", run `cw_runtime --diag`
and paste its output:

```
```
